title: the sleep chronicles
description: Logging my sleep journey.
url: "https://aidencullo.github.io"
baseurl: "/the-sleep-chronicles"

theme: minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag

permalink: /:year/:month/:day/:title/

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
